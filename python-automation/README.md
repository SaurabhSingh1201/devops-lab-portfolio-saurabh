# Python EC2 Automation

This script automates the starting and stopping of EC2 instances based on tags.

## Requirements

- `boto3` library installed (`pip install boto3`).
- AWS credentials set up.

## Usage

1. Change the tag key and value in the script.
2. Run the script to start/stop EC2 instances:
   ```bash
   python ec2_automation.py